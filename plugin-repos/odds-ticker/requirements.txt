# Odds Ticker Plugin Requirements

# Hard imports in manager.py — must be installed even though some
# overlap with the LEDMatrix core deps. The plugin loader installs
# this file on first load, so listing them explicitly avoids
# ImportError on hosts where the host environment is minimal.
numpy>=1.20.0
pytz>=2023.3
requests>=2.25.0
Pillow>=8.0.0
